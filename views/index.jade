extends layout

block content
  h1= title
  p Welcome to #{title}
  p Environment Variables:
    ul
      li NODE_ENV: #{process.env.NODE_ENV}
      li ROLLBAR_ACCESS_TOKEN: #{process.env.ROLLBAR_ACCESS_TOKEN}
